"use client";
import * as React from "react";import { use } from "react";import { api,ApiError } from "@/lib/api/client";import { EmptyState } from "@/components/common/empty-state";import { TableSkeleton } from "@/components/common/loading-skeleton";import { DigitalProductForm } from "@/components/digital/digital-product-form";import type { DigitalConfig } from "@/components/digital/types";
interface ProductResponse{id:string;name:string;slug:string;sku:string;status:string;shortDescription?:string|null;description?:string|null;categoryId?:string|null;brandId?:string|null;regularPrice:string;salePrice?:string|null;metaTitle?:string|null;metaDescription?:string|null;digitalConfig?:Partial<DigitalConfig>|null;images?:{id:string;url:string}[]}
export default function EditDigitalProductPage({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[product,setProduct]=React.useState<ProductResponse|null>(null);const[error,setError]=React.useState<string|null>(null);const[loading,setLoading]=React.useState(true);React.useEffect(()=>{let cancelled=false;api.get<ProductResponse>(`/commerce/products/${id}`).then((result)=>!cancelled&&setProduct(result)).catch((err)=>!cancelled&&setError(err instanceof ApiError?err.message:"Unable to load product")).finally(()=>!cancelled&&setLoading(false));return()=>{cancelled=true}},[id]);if(loading)return <TableSkeleton rows={8} cols={2}/>;if(!product||error)return <EmptyState title="Digital product not found" description={error??"This product is no longer available."}/>;return <DigitalProductForm initialProduct={product}/>}
