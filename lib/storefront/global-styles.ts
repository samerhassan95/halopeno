export interface GlobalStylesConfig {
  primary: string;
  accent: string;
  background: string;
  radius: string;
}

export const DEFAULT_GLOBAL_STYLES: GlobalStylesConfig = {
  primary: "#124b2d",
  accent: "#b52a24",
  background: "#ffffff",
  radius: "1.5rem",
};
